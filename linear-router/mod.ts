/**
 * Linear Router - Default router implementation for Tabi applications.
 *
 * Uses O(n) linear search for route matching. Simple, predictable, and fast startup.
 *
 * @module
 */

export { LinearRouter } from "./linear-router.ts";
